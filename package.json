{
  "name": "media-sniper",
  "version": "0.1.0",
  "private": true,
  "description": "MV3 media sniffer/downloader: detects videos, HLS streams and audio from any site using the browser's own logged-in session.",
  "scripts": {
    "test": "node test/logic.test.js && node test/logic2.test.js && node test/bridge.test.js && node test/background.test.js",
    "check": "for f in src/*.js popup/*.js test/*.js; do node --check $f || exit 1; done",
    "zip": "node scripts/pack.js"
  }
}
